pre-commit:
  parallel: true
  jobs:
    - name: fmt
      glob: "*.{md,mdx}"
      run: pnpm fmt {staged_files}
      stage_fixed: true

    - name: lint
      glob: "*.{md,mdx}"
      run: pnpm exec textlint {staged_files}
